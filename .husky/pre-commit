# Lint front-end if any front-end files are staged
STAGED_FE=$(git diff --cached --name-only --diff-filter=ACM -- 'front-end/')

if [ -n "$STAGED_FE" ]; then
  echo "Running ESLint on front-end..."
  cd front-end
  npm run lint
fi
